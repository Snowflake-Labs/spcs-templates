apiVersion: 1

providers:
  - name: First # A uniquely identifiable name for the provider
    folder: Snowpark Container Services # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/compute_pool_view.json

  - name: Second # A uniquely identifiable name for the provider
    folder: Snowpark Container Services # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/services_view.json
